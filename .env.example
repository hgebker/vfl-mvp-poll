# Drizzle
DATABASE_URL=local.db

# Session cookie signing secret (generate with: openssl rand -hex 32)
SESSION_SECRET=

# Bearer token for devops-only HTTP endpoints (generate with: openssl rand -hex 32)
DEVOPS_TOKEN=
